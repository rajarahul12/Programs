#include<bits/stc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string s;
    getline(cin,s);
    int i,len=s.length();
    for(i=0;i<len;i++){
    	if(s[i] == )
    }
    return 0;
}
